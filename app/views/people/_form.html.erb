isso é pro select funcionar com materialize <%= form_with model: @person, local: true do |form| %> <% if @person.errors.any? %>
        <%= pluralize(@person.errors.count, "error") %> prohibited this article from being saved:

        <% @person.errors.full_messages.each do |msg| %>
            <%= msg %>
        <% end %>
    <% end %>
    <%= form.label :name %>
    <%= form.text_field :name %>

    <%= form.label :user_id %>
    <%= form.text_area :user_id %>

    <%= form.label :birth_date %>
    <%= form.text_area :birth_date %>

    <%= form.label :home_address %>
    <%= form.text_area :home_address %>

    <%= form.label :ninja_rank %>
    <%= form.select :ninja_rank, @ranks %>

    <%= form.label :username %>
    <%= form.text_area :username %>

    <%= form.label :user_type %>
    <%= form.text_area :user_type %>

    <%= form.label :email %>
    <%= form.text_area :email %>

    <%= form.submit %>

<% end %>