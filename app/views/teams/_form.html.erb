<%= form_with model: @team, local: true do |form| %>

    <% if @team.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@team.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @team.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= form.label :Name %><br>
      <%= form.text_field :name %>
    </p>

    <p>
      <%= form.label :Ninja  %><br>
      <%= form.text_area :person1_id%>
    </p>

    <p>
      <%= form.label :Ninja %><br>
      <%= form.text_area :person2_id%>
    </p>

    <p>
      <%= form.label :Ninja %><br>
      <%= form.text_area :person3_id %>
    </p>

    <p>
      <%= form.label :Ninja_Superior %><br>
      <%= form.text_area :personsuperior_id %>
    </p>

    <p>
      <%= form.submit %>
    </p>
<% end %>