<%= form_with model: @team, local: true do |form| %>

    <% if @team.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@team.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @team.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= form.label :name %><br>
      <%= form.text_field :name %>
    </p>

    <p>
      <%= form.label :Ninja  %><br>
      <%= form.select :person1_id, options_for_select(@people.map{|p| [p.name, p.id]})%>
    </p>

    <p>
      <%= form.label :Ninja %><br>
      <%= form.select :person2_id, options_for_select(@people.map{|p| [p.name, p.id]})%>
    </p>

    <p>
      <%= form.label :Ninja %><br>
      <%= form.select :person3_id, options_for_select(@people.map{|p| [p.name, p.id]})%>
    </p>

    <p>
      <%= form.label :Ninja_Superior %><br>
      <%= form.select :personsuperior_id, options_for_select(@chuninmais.map{|p| [p.name, p.id]})%>
    </p>

    <p>
      <%= form.submit %>
    </p>
<% end %>